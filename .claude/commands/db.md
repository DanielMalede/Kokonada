---
description: Query the Kokonada MongoDB (Atlas) — needs the MongoDB MCP connected.
argument-hint: [what to look up]
---
Using the MongoDB MCP, inspect/answer: $ARGUMENTS. Read-only unless I explicitly ask for a write. Show the exact query you ran and the result. If the MongoDB MCP isn't connected, tell me and give me the read-only mongosh/railway command to run instead.
