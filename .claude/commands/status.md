---
description: Status check — which wave/stage am I in and what's left, from the real docs + git.
---
Read the actual project state, don't answer from memory. ultrathink.
Read docs/MASTER_BLUEPRINT_2026-07-07.md (all waves + the DEFERRED section), docs/GROUND_TRUTH_2026-07-07.md, docs/VISION.md, and the live state — `gh pr list --state all` (recent), `gh issue list`, and `git log` on main since last session. Cross-reference the Blueprint waves against what actually merged.
Give a precise "YOU ARE HERE" report: 1) current wave + sub-stage with evidence (which PRs merged/open); 2) DONE (with the PRs that closed each); 3) IN-FLIGHT (open PRs/branches + exactly what each is blocked on); 4) BLOCKED (what's held and why); 5) THE one immediate next action; 6) what's left to launch, flagging decisions I still owe. Cite wave IDs, task IDs, PR numbers. Stop after the report — no work.
