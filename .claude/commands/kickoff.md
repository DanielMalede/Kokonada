---
description: Session-start protocol — Fable reviews the project and recommends the one job.
---
Run the session-start protocol from docs/ORCHESTRATOR_FABLE.md. First read docs/VISION.md and docs/SCREENS.md, then re-orient from saved state (GROUND_TRUTH, MASTER_BLUEPRINT, ADRs, MEMORY, open PRs/issues, git log since last session) — no blind full-scan. Give a Session Health Report (where we are; flaws/bugs/drift; dead code + add/delete candidates marked "DISCOVERED"; anything the roadmap misses), then recommend THE one job to do now with rationale + blast radius. ultrathink. Stop at the recommendation gate — no squads, no code.
