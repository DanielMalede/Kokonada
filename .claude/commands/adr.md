---
description: Record an Architecture Decision Record.
argument-hint: [the decision]
---
Write an ADR in docs/adr/ for: $ARGUMENTS. Use the next number in sequence. Format: Context → Decision → Consequences (+ alternatives considered + reversibility). Keep it lean. Cross-reference the relevant doc (VISION / ORCHESTRATOR_FABLE / RUNTIME_AGENT_ARCHITECTURE / blueprint). Docs-only; show me the file.
