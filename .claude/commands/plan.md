---
description: Architect a task — decision tables + dependency-ordered plan (no edits).
argument-hint: [what to plan]
---
Use the architect agent to plan: $ARGUMENTS. ultrathink. Read-only — no edits. Establish ground truth from code/tests; map the slice; surface any fork as a decision table (options + blast radius + reversibility + recommendation); produce a dependency-ordered, TDD-ready plan (per task: scope, files, acceptance criteria, blast radius, verification). End with "Plan ready — approve before implementation."
