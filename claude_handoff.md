# Kokonada — Session Handoff (updated 2026-07-17, later same day)

Everything below is committed/merged unless noted. Safe to `/clear` and resume from this.
This file is a **cross-session** handoff — multiple parallel sessions update it. Always
`git fetch origin main` and re-verify PR/merge state before trusting a status here; this repo
moves fast (5+ PRs merged in a single day is normal) and any entry can go stale within hours.

## ✅ Shipped & merged (main)
- **#140** Spotify null-guard — `SpotifyRemote.configure` no longer red-boxes launch on incomplete config; warns + skips instead.
- **#141** Onboarding/FTUE (§2) + Splash (§1) — 4-state route machine, soft-glow bioluminescent aura (new `SoftGlow.tsx` Skia primitive), colorblind-safe carousel. Designer SHIP on on-device recaptures, resilience 8.5/10, on-device logout→relaunch blocker check passed.
- **#142** Genre-Jaccard discovery seam (mbid: representation) — activated the dormant genre-relevance scoring seam behind `DISCOVERY_GENRE_RELEVANCE` (default off in code, **flipped ON in prod at `DISCOVERY_GENRE_WEIGHT=0.35`** after a band-aware prod composition sweep proved the lift: moderate 6.7%→26.7%, happy-dance 16.7%→26.7%, sad-acoustic 23.3%→30.0%, all monotonic, zero out-of-band admissions since the rerank runs after the biosonic band filter). Preserves the #139 genre-free-vector invariant. `backend/app/scripts/measureDiscoveryComposition.js` is the reusable read-only composition-sweep tool if this needs re-tuning later.
- **#151** YouTube corpus containment — mirrors PR #147's Spotify containment across all 6 write-gates (not just the 3 originally scoped: `musicProfileService.js`, `catalogAndEmbed.js`, `toCatalogEntry.js`, `featureService.js`, `embedding.worker.js`, + new `youtubeLeakMonitor.js`/`purgeYoutubeCorpus.js`). Independent compliance SHIP, resilience 9.0/10. **Purge already run** — 7,918 leaked rows deleted (was ~33-36% of the whole corpus), `/health/youtube-leak` confirmed `{ok:true,total:0}`.
- **#153** YouTube mobile OAuth callback — `youtubeConnect`/`youtubeCallback` now thread `returnTo=app` and deep-link back into the app on both success and failure, mirroring the already-working Spotify pattern. Resilience 9.0/10.
- **#154** WS-5 — GDPR Art.9 health-consent gate (audit finding H-9, **now fully closed**). New versioned/append-only `ConsentRecord` + `/api/consent*` API + `requireConsent` server gate on every user-facing special-category ingestion path (`/health/batch`, `/apple/push`, and — the one gap a resilience audit caught post-build — the device-token-authed `/watch/hr` live-BLE lane, gated inline since it has no session `req.user`). Mobile: a just-in-time consent screen before the OS Health Connect prompt (equal-weight Accept/Decline, no dark patterns; the OS sheet opens ONLY on a server-acked current grant, spy-tested). Also closed in the same PR: a `latestFor` cross-replica same-millisecond tie-break (fails closed → prefers `withdrawn`), and a cross-package consent-version contract (`CONSENT_SCREEN_VERSION` on the client; server 409s a mismatch instead of silently trusting a stale client's grant). Reconciled against PR #152's concurrent Health Connect scope-minimization (dropped SpO₂/respiratory/background-read — zero readers existed). Backend 155/156 suites, mobile 81/81 — both green (the one backend "failure" is the pre-existing no-local-Redis `worker.test.js`, unrelated). `docs/PRIVACY_DECLARATIONS.md` + new `docs/store/play-health-connect-declaration.md` + `docs/SCREENS.md` §11 all refreshed to match.
- **#155** Spotify resolver URI recontamination — removed a fire-and-forget cache-back write in `biometricHandler.js` that was re-leaking `spotify:` URIs onto the shared `TrackCatalog` (a gap PR #147 missed). `translateToSpotify`/playback path fully preserved. Resilience 9.5/10, independent compliance SHIP.

## 🔵 Open PRs (not mine — from other parallel sessions, unverified by me)
- **#156** `fix/garmin-disconnect-erasure-wiring` — "wire the real wearable-disconnect routes through the erasure module (Garmin data was never actually purged)".
- **#157** `fix/youtube-discovery-shortcircuit` — "skip wasted LLM/vector calls for YouTube-only users before the guaranteed-empty discovery check".

## ✅ Prod ops run this session (Daniel-authorized, both verified complete)
- **Grandfather consent backfill** (`node scripts/backfillGrandfatherConsent.js`, post-#154) — Daniel's own account had pre-consent-gate Health Connect usage; backfilled one `ConsentRecord` (granted, v1) so the new `/health/batch` gate doesn't lock out his existing sync. Verified via direct Mongo read.
- **`node scripts/purgeSpotifyCorpus.js --apply`** (the item flagged as "post-merge, non-urgent" below is now DONE) — dry-run reviewed first (1,638 TrackCatalog / 2,439 TrackEmbedding / 2,439 AudioFeature rows, ~18% of catalog), then applied. **Confirmed complete and clean**: all three collections dropped from ~9,080–10,154 rows to exactly **4,957** (the CC0/`mbid:` corpus, fully intact), zero `spotify:`-prefixed rows remain anywhere, zero `af:spotify:*` Redis keys needed purging. Verified two ways — the script's own flushed completion report AND independent read-only Mongo queries against all three collections. **Gotcha:** the script hung ~30 min post-completion on `railway run`'s local-tunnel Redis handshake/teardown (same class of stall as the dry-run's ~15 min Redis-connect delay) — Railway's own service logs confirmed Mongo/Redis are instantly reachable from *inside* Railway's network, so this is a `railway run` tunnel-specific slowness, not an infra problem. Diagnosed via read-only Mongo counts (which had already dropped to the post-purge numbers) before safely force-killing the hung local process — do this same diagnostic (re-check the target collection's live count) before killing a similarly-hung `railway run` write next time, so you're not guessing whether a kill is safe.

## ⏳ In progress — Daniel-paced, not blocking anything else
- **Google OAuth sensitive-scope verification for YouTube** — checklist + copy-ready Groq privacy-policy disclosure delivered as an Artifact: https://claude.ai/code/artifact/c25eaf95-44b4-4f8c-a8ee-6192c115906b. Key finding: `youtube.readonly` is **sensitive**, not **restricted** — no CASA audit needed, lighter path than feared. One trap flagged: move the app to "Production" status before starting the review clock, or sensitive-scope tokens expire every 7 days in "Testing." Daniel is working this in Google Cloud Console at his own pace.

## 📋 Next up — Connect Services (§4), Wave 2.8.2 queue item #2
Full architect plan exists (provider registry `{enabled|deferred|halted}` so providers flip without screen rework). NOT yet approved for build — blocked on:
- **Spotify → `halted`.** Corpus-drip side is now code-fixed (#147 + #155), but Spotify's own Feb-2026 Dev-Mode crackdown caps free/solo-dev apps at **5 authorized users total**, universally enforced since March 9 2026. Extended Quota needs a registered business + 250k MAU. **No known code fix — this is an external, currently-unresolvable blocker.**
- **YouTube → `deferred`, down to ONE blocker.** Corpus containment DONE, mobile callback DONE. Only the OAuth verification above remains — once that completes, YouTube can flip to `enabled` with zero screen rework.
- Wearable/Health + "Try with mood only" are clear to build now if you want to start on those independently of the two blockers above.

## Environment / gotchas
- **Worktree hygiene:** this session created and cleaned up `.worktree-spotify-guard`, `.worktree-youtube-containment`, `.worktree-youtube-callback` (all merged, removed). `.worktree-spotify-resolver` **still exists on disk though PR #155 already merged** — likely safe to remove now, but not force-deleted automatically since I can't confirm no other session is still using it (don't touch a live worktree without checking). `mobile/KokonadaHealth/.worktree-onboarding` still exists with some stray uncommitted debug artifacts (corrupted screenshot files, an accidental edit to the *unused* `mobile/src/health/config.ts`) — safe to clean up, just not force-deleted automatically since it had uncommitted changes.
- **Other parallel sessions are active in this repo** — the main checkout has been observed on `feat/global-seed-ingestion`, `feat/discovery-genre-relevance`, and `feat/ws5-consent-backend` at different points this session, and worktrees `kokonada-hotfix-dbname`/`kokonada-wave5`/`kokonada-wave6`/`kokonada-youtube-playback` exist that this session didn't create. Don't assume the main checkout's current branch reflects this session's work — always fetch/verify against `origin/main` directly.
- **On-device mobile debugging (Windows/Git Bash):** `adb shell screencap`/`adb pull` piped through PowerShell `>` corrupts PNGs (UTF-16 re-encoding) — use `adb pull` to a real file instead. Git Bash mangles `/sdcard/...` device paths (MSYS path conversion) — prefix commands with `MSYS_NO_PATHCONV=1` and use Windows-style host destinations (`C:/Users/...`) for `adb pull`.
- **A worktree cut from `origin/main` has a placeholder `mobile/KokonadaHealth/src/health/config.ts`** (no real `SPOTIFY_REDIRECT_URI`, dummy backend/Google values) — copy the real one from the main checkout + `git update-index --skip-worktree` before an on-device build, or the app red-boxes at launch (now degrades gracefully instead of crashing, post-#140, but still won't have working Spotify/Google/backend without the real values).
- **The `gh pr merge` classifier hard-blocks agent-initiated merges** regardless of verbal permission — this is by design, not a bug; every PR in this session needed Daniel's manual merge click.
- **MongoDB MCP tool is connected to the real app database** (named literally `test` — Mongoose's default when `MONGO_URI` doesn't specify a db name — confirmed by exact schema match: `trackcatalogs`, `audiofeatures`, `trackembeddings`, `musicprofiles`, `users`, etc.). Read-only `count`/`find` are safe and were used this session to independently verify leak scale before/after the YouTube purge.
- Commit style: **short single-line, no body/trailers, no AI/Claude/Anthropic attribution** (CLAUDE.md standing order).
