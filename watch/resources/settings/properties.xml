<properties>
    <!-- Backend base URL. Defaults to production; override for local dev. -->
    <property id="backendUrl" type="string">https://kokonada-backend-production.up.railway.app</property>
    <!-- DEVICE TOKEN — leave as the placeholder here. Enter the real whr_… token on your
         PHONE via Garmin Connect → this app → Settings (it's a user setting; see settings.xml),
         which overrides this default at runtime. Do NOT paste a real token into this tracked
         file. This file is also marked skip-worktree locally (see watch/README.md) so a local
         paste can't be committed by accident. -->
    <property id="watchToken" type="string">YOUR_TOKEN_HERE</property>
</properties>
